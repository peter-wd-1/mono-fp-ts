{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/cicd/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/cicd",
        "tsConfig": "libs/cicd/tsconfig.lib.json",
        "packageJson": "libs/cicd/package.json",
        "main": "libs/cicd/src/index.ts",
        "assets": [
          "libs/cicd/*.md",
          {
            "input": "./libs/cicd/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./libs/cicd/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          },
          {
            "input": "./libs/cicd",
            "glob": "generators.json",
            "output": "."
          },
          {
            "input": "./libs/cicd",
            "glob": "executors.json",
            "output": "."
          }
        ]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/cicd/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/cicd"],
      "options": {
        "jestConfig": "libs/cicd/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/cicd/tsconfig.lib.json -w"
      }
    },
    "commit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "git add . && yarn cz"
      }
    },
    "commit-pr": {
      "executor": "nx:run-commands",
      "options": {
        "command": "gh pr create"
      }
    },
    "release": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn run release && git push --follow-tags origin main"
      }
    },
    "null": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo null command executed"
      }
    },
    "dev": {
      "executor": "@rfiready/cicd:cd",
      "outputs": ["{options.outputPath}"],
      "options": {
        "env": "dev"
      }
    },
    "deploy": {
      "executor": "@rfiready/cicd:cd",
      "outputs": ["{options.outputPath}"],
      "options": {
        "env": "prod",
        "action": "deploy"
      }
    },
    "make-deploylist": {
      "executor": "@rfiready/cicd:cd",
      "outputs": ["{options.outputPath}"],
      "options": {
        "env": "prod",
        "action": "make"
      }
    },
    "push-deploylist": {
      "executor": "@rfiready/cicd:cd",
      "outputs": ["{options.outputPath}"],
      "options": {
        "env": "prod",
        "action": "push"
      }
    },
    "serve-deploylist": {
      "executor": "@rfiready/cicd:cd",
      "outputs": ["{options.outputPath}"],
      "options": {
        "env": "prod",
        "action": "serve"
      }
    }
  },
  "tags": []
}
