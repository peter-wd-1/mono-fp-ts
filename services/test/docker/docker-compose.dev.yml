version: "3.9"

services:
  test:
    build:
      context: ../../../
      dockerfile: ./services/test/docker/Dockerfile.dev
      args:
        NODE_ENV: prod
        SERVICE_NAME: test
        PORT: 1234
    working_dir: /app
    ports:
      - 1234:1234
    volumes:
      - ../../operation:/app/services/test
