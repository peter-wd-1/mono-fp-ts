version: "3.9"

services:
  test:
    build:
      context: ../../../
      dockerfile: ./services/test/docker/Dockerfile.dev
      args:
        NODE_ENV: prod
        SERVICE_NAME: test
        PORT: 3332
    working_dir: /app
    ports:
      - 3332:3332
    volumes:
      - ../../operation:/app/services/test
